import React from 'react';

// This generic loader is no longer used.
// All components now implement their own custom, context-aware loading states.
const Loader: React.FC = () => null;

export default Loader;
